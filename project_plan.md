# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제: 리듬게임 만들기

---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.


내 설명: 사용자가 박자에 맞게 키를 누르며 즐길수 있어야 한다.


---

## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

- 사용자가 선택한 게임 난이도 번호 (1, 2, 3 또는 종료 4)

-게임 플레이 중 화면에 제시된 노트를 보고 사용자가 누르는 키 입력값 (Q, W, E, R)

-사용자의 입력 순간을 측정하기 위한 시스템 현재 시간 데이터 (time.time())

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

-난이도별 매개변수 할당: 선택한 레벨에 따라 제한 시간과 시작 목숨 개수를 다르게 세팅

-중복 방지 노트 생성 알고리즘: random.shuffle()을 활용하여 Q, W, E, R 키가 4번 연속 중복되지 않도록 2차원 리스트에 노트 데이터 저장

-시간 초과 및 정답 판정: 입력 시간(종료시간 - 시작시간)이 제한 시간을 넘었는지 검사하고, 입력한 키가 정답 키와 일치하는지 판정하여 목숨(life) 차감 및 점수(total_score) 계산

-서바이벌 탈출 조건 처리: 게임 도중 목숨이 0이 되면 반복문을 즉시 탈출(break)

-최종 점수 기반 등급 계산: 획득 점수에 따라 A, B, C, D, F 등급으로 데이터 분류

### 출력

프로그램이 최종적으로 무엇을 출력하는가?

-난이도 선택 메뉴판 및 레벨 세팅 안내 메시지

-게임 시작 전 시각적 몰입감을 주는 3, 2, 1 카운트다운 연출

-실시간 게임 상황 (현재 노트 번호, 남은 목숨 하트(❤️) 개수, 정답 키 힌트)

-정답/오답/시간 초과에 따른 실시간 판정 결과 메시지 및 소요 시간

-게임 종료 후 최종 누적 점수와 점수대별 최종 성취 등급 (🏆 A등급 등)
---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름:


### 한 행의 의미
게임에서 순차적으로 등장할 '개별 노트 하나의 상세 정보 세트(노트 번호, 정답 키, 성공 시 배당 점수)'를 의미한다.
### 각 열의 의미

열 번호|의미|데이터 타입|예시
0번 열|노트의 진행 순서를 나타내는 번호,정수형 (int)|"1, 2, 3"
1번 열|사용자가 입력해야 하는 무작위 정답 키,문자열 (str)|"""Q"", ""W"", ""E"", ""R"""
2번 열|해당 노트를 제한 시간 내에 맞췄을 때 획득하는 점수,정수형| (int),20

---

## 5. 함수 설계

함수 이름|역할|입력값|반환값 또는 출력
show_menu()|메인 화면에서 난이도 메뉴를 출력하고 사용자의 레벨 선택을 받아 검증함,없음|"선택한 레벨 번호 (1, 2, 3) 또는 종료 번호 (0) 반환"
play_rhythm_game()|"중복 없는 12개 노트를 생성하고, 카운트다운 후 입력 시간과 정답을 체크하여 서바이벌 게임을 진행함|","level_name, limit_time, initial_life"|게임 종료 후 누적된 total_score 반환 및 등급 화면 출력
calculate_grade()|사용자가 최종적으로 획득한 점수를 기반으로 성취 등급을 판정함,score (최종 점수)|등급 이름과 칭찬 멘트가 담긴 문자열 (str) 반환

---

## 6. 사용할 제어구조

### 조건문을 사용할 부분

내 계획:

메인 루프 제어: 사용자가 메뉴에서 4번을 입력하면 if selected_level == 0: 조건을 만족하여 전체 게임 무한 루프를 완전히 탈출(break)하도록 제어한다.

다중 조건 난이도 세팅: if-elif 문을 사용하여 사용자가 선택한 레벨에 따라 제한 시간(limit_time)과 목숨(initial_life)을 다르게 차등 할당한다.

복합 판정 로직: 사용자의 입력 소요 시간이 제한 시간을 초과했는지 먼저 검사(if elapsed_time > limit_time)하여 감점 및 목숨을 깎고, 시간 내에 입력했다면 대소문자 구분 없이 정답과 일치하는지(if user_input.upper() == correct_key:) 판정한다.


### 반복문을 사용할 부분

내 계획:

게임 프로그램 무한 재실행: while True: 문을 사용하여 사용자가 완전히 종료(4번)를 선택하기 전까지는 게임이 끝난 후 다시 시작 메뉴 화면으로 돌아와 무한히 재시작할 수 있는 게임 루프를 구현한다.

시작 카운트다운: for count in range(3, 0, -1): 반복문과 time.sleep(1)을 조합하여 숫자가 3, 2, 1로 1초씩 줄어들며 대기하는 카운트다운을 구현한다.

2차원 리스트 생성 및 순회: for문을 사용하여 12개의 노트를 셔플 방식으로 생성해 2차원 리스트에 담고, 다시 for note in game_notes:를 통해 순차적으로 노트를 꺼내오며 사용자의 입력을 유도한다.

중복 방지 풀(Pool) 리프레시: 노트 생성 도중 4개의 키를 다 써서 바구니가 비게 되면 if not current_pool: 조건을 만족하여 다시 QWER을 채우고 random.shuffle()을 반복 수행한다.


---

## 7. 예상되는 예외 상황

예) 조건에 맞는 추천 결과가 없을 때, 잘못된 입력을 했을 때 등

1. 메뉴 선택 시 잘못된 문자나 범위 밖의 숫자 입력

해결 방안: show_menu 함수 내의 else: 구문을 통해 에러 메시지를 출력하고, while문에 의해 올바른 번호(1~4)를 누를 때까지 메뉴를 무한 반복해서 다시 띄운다.

2. 사용자가 키 입력 시 소문자(q, w, e, r)를 입력하는 상황

해결 방안: 파이썬은 대소문자를 구분하므로 소문자를 오답 처리할 수 있다. 이를 방지하기 위해 사용자 입력값 뒤에 .upper() 메서드를 붙여 무조건 대문자로 변환한 뒤 정답 키와 비교하도록 예외를 처리했다.

3. 함수 정의와 호출 순서 뒤바뀜으로 인한 실행 에러 (NameError)

해결 방안: 파이썬의 하향식 독해 특성에 맞춰 실제 게임이 구동되는 while True: 메인 실행부를 소스 코드의 가장 최하단(맨 아래)에 배치하여 구조적 예외를 해결했다.

---

## 8. Gemini에게 도움받고 싶은 부분

예) 주제 구체화, 2차원 리스트 구조, 함수 분리, 오류 수정 등

1. 기본 코드 제작
2. 혹시 더 수정해야하는 부분이 있는지 검토
3. 아이디어 실현
